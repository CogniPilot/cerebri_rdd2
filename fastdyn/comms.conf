# Optional network profile for native simulation diagnostics.
# Lockstep request/response pacing remains on direct shared memory. ROS 2 CDR
# mirrors run on the host and do not require an embedded transport bridge.
CONFIG_NETWORKING=y
CONFIG_MDIO=y
