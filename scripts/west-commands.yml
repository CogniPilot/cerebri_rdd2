# Keep the help strings in sync with the values in the .py files!
west-commands:
  - file: scripts/west_commands/format.py
    commands:
      - name: format
        class: Format
        help: format tracked C/C++ files with pinned clang-format
