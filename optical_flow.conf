# SPDX-License-Identifier: Apache-2.0

# Opt-in flight profile: retain onboard GPS and fuse a gPTP-synchronized,
# range/tilt-compensated planar velocity from the shaped sensor node.
CONFIG_RDD2_OPTICAL_FLOW_SOURCE_CSYN=y
